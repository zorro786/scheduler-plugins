apiVersion: v1
kind: Pod
metadata:
  generateName: pod-
spec:
  containers:
  - image: k8s.gcr.io/pause:3.2
    name: pause
    ports:
    - containerPort: 80
    resources:
      limits:
        cpu: 1000m
        memory: 500Mi
      requests:
        cpu: 1000m
        memory: 500Mi
